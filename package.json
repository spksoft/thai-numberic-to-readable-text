{
  "name": "thai-numberic-to-readable-text",
  "version": "0.0.1",
  "repository": "spksoft/thai-numberic-to-readable-text",
  "description": "thai-numberic-to-readable-text is a js libery to convert numberic to text in thai ",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "author": "Sippakorn Raksakiart (spksoft)",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node dist/index.js",
    "test": "jest"
  },
  "dependencies": {
    "typescript": "^3.6.2"
  },
  "publishConfig": {
    "access": "dist"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.4",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2"
  }
}
